{
    "version": "0.2.0",
    "configurations": [
      
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome",
        "url": "http://localhost:8080", // Adjust to your application's URL
        "webRoot": "${workspaceFolder}",
        "sourceMaps": false // Disable source maps to avoid the error
      }
    ]
  }
  